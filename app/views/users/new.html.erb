<% provide(:title, 'Sign up') %>
<% provide(:btn_text, '新規作成') %>
<% provide(:btn_class, 'signup') %>
<h1>ユーザー登録</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_with(model: @user, local: true) do |f| %>
      <%= render '/users/form' %>
    <% end %>
  </div>
</div>
